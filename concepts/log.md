# 概念日志

## 2026-08-24

- 摄取 source-008（luyao 关于菩提的三个小感悟）后新增 3 个概念：`闲聊是土壤`（土壤是人待在一起的空间，建设从闲聊和关系里长出，投机在链末尾）、`代币即频道`（被当频道用是种子形态而非退化/误用）、`兴趣指南针`（Paul Graham 筛选原则移植：不看规模、以兴趣和直觉为指南针）。
- 补强 `热闹大于整齐`（土壤论把热闹从界面层推进到生长机制层）、`反预判策略`（兴趣指南针是其筛选层具体化）、`群体基础与时机`（频道是群体基础的种子阶段）。均不映射到既有机制章节，`chapter_refs` 沿用原状。
## 2026-08-05

- 摄取 source-007（luyao 新版 UI 设计说明）后新增 3 个概念：`四种核心操作`（买/卖/发/给，买/卖只是其中一部分）、`代币能量系统隐喻`（买卖=外界能量交换、给币=系统内循环、做事=做功，决定电池还是有机体）、`代币活跃度指标`（事件柱状图替代看不见活跃度的传统 K 线）。
- 补强 `热闹大于整齐`（新增 UI 落地证据：列表微型图、个人页语境化）和 `行动与给币`（新增给币被 UI 显著呈现的证据）。三者 `chapter_refs` 留空或沿用，均不映射到既有机制章节。

## 2026-07-21

- 摄取 `source-006`（luyao 关于 Cursor 多智能体实验的随想）后新增两个概念：`领导者与执行者分工`（中心化成本最优分工，对照组）、`去中心化效率命题`（Bodhi 总命题，实验侧）。两者成对使用，把 Bodhi 的机制设计统一到“验证去中心化能否高效”之下。不映射到现有 chapters，`chapter_refs` 留空。

## 2026-07-13

- 摄取 `source-005`（luyao 回答：发项目是否等于发币）后新增两个概念：`低成本发币的监管模糊性`、`创建者与代币关系模糊化`；并补强 `代币即无工资公司`（“每天创建一家公司”）。
- 摄取 `source-004`（创建者回答用户提问）后新增两个机制概念：`两种盒子模型`、`市场驱动可信度`；并用其补强 `小型贡献市场` 的“为什么是这种形态”段落。
- 摄取 `source-003`（Bodhi Thesis）后新增四个概念：`反预判策略`、`正方形哲学`、`体验优于逻辑`、`热闹大于整齐`。均为创始人叙事/方法论，不映射到现有 chapters，`chapter_refs` 留空。

## 2026-07-12

- 收紧 source-001 系概念页的 `chapter_refs` 到证据实际所在章：`代币即无工资公司`→[ch00]、`行动与给币`→[ch02]、`启动曲线阶段`/`迁移与流动性锁定`→[ch01]、`无管理员合约规则`→[ch00,ch04]、`小型贡献市场`→[ch02]、`辅助基础设施`→[ch02,ch03,ch04]；跨源 `代币现实反馈回路`→[ch02,ch05]。

## 2026-06-30

- 新增城市代币、无市长城市、给钱与给币的激励差异、群体基础与时机。
- 扩展代币现实反馈回路，使其覆盖城市代币案例。
- 初始创建。
